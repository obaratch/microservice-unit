# npm.md

このドキュメントでは、npmの使用に関する共通の方針を定める。

## 基本方針
- パッケージの更新は極力 `npm` コマンドだけで行い、手動で `package-lock.json` を編集しない。
- `npm` コマンドはサンドボックス制約下にあるため、基本的にユーザーに操作を依頼する。
  * `npm -i react react-dom` のように、ワンライナーを提示してくれるとユーザーは助かる。
