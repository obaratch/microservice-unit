# git.md

このドキュメントでは、Gitの使用に関する共通の方針を定める。

## コミットメッセージ

- Conventional Commits 風に `<type>: <内容>` の形式を基本とする。
- `<内容>` は日本語をデフォルトとするが、英語でも構わない。
- 作業途中のコミットは `wip: 〇〇作業中` の形式にする。
  - 例: `wip: ログイン画面作業中`
